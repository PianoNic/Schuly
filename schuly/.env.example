# Sentry/GlitchTip Configuration
# These values are automatically set in CI/CD from GitHub repository variables
# For local development, pass these as --dart-define parameters

# Your GlitchTip DSN (Data Source Name)
# Stored in GitHub as repository variable SENTRY_URL
SENTRY_DSN=

# Release version (automatically set from git tag in CI/CD)
SENTRY_RELEASE=dev

# Environment name (production for releases, development for local)
SENTRY_ENVIRONMENT=development